// Animations Module
const AnimationsModule = {
    init() {
        console.log('Animations module initialized');
        // Aggiungi qui logiche di animazione globali
    }
};

document.addEventListener('DOMContentLoaded', () => {
    AnimationsModule.init();
});