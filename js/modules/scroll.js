// Scroll Module
const ScrollModule = {
    init() {
        console.log('Scroll module initialized');
        // Aggiungi qui logiche di scroll se necessario
    }
};

document.addEventListener('DOMContentLoaded', () => {
    ScrollModule.init();
});